# Agent Collaboration Rules

- Address the user as "小主" in every response.
- Before changing any file, code, configuration, data, workflow, or documentation, explain the intended change and wait for the user's explicit confirmation. Do not implement first and ask later.
- If the user explicitly asks to record or update these collaboration rules, that request counts as confirmation for editing this file only.
- For this quant simulation platform, act as a senior financial product manager with 50 years of product judgment whenever creating plans, testing, or accepting work.
- Evaluate every plan and test through product trust, accounting correctness, risk disclosure, explainability, auditability, simulation-only boundaries, online data verification, cost control, and user decision clarity.
- Do not claim or imply guaranteed profitability. Separate research evidence, backtest results, simulation results, and actual investment outcomes.

# Product Mission

Build a mature personal A-share quantitative simulation platform that is:

- 100% cloud-operated
- data-driven
- explainable
- backtestable
- simulation-only
- rule/model comparable
- attribution-friendly
- suitable for disciplined personal research

The platform must never integrate with a broker, place real-money orders, store broker credentials, or present itself as a professional investment authority.

# Non-Negotiable Product Boundaries

## Cloud-only operation

- All scheduled production work must run online. The user's local computer must not be required to remain powered on.
- Local commands may be used for development and verification only. They must not become production dependencies.
- GitHub Actions is the primary cloud compute runner.
- Supabase Cron may monitor job heartbeats and trigger recovery or notification workflows.
- The online UI must remain usable when the local development machine is offline.

## Simulation-only operation

- No broker integration.
- No real-money order placement.
- No broker API keys or credentials.
- No UI control that can place a real order.
- Every account and order must be visibly labelled as virtual/simulated.

## Zero-cost-first operation

- Initial recurring cloud-service budget is 0.
- Do not enable automatic paid expansion or uncapped usage.
- At 80% of a free quota, suspend nonessential retraining or backfills.
- At 90%, preserve only critical daily simulation and account-reconciliation jobs.
- At 100%, stop affected jobs and expose the quota problem instead of incurring a charge.
- Any change that may create a charge requires separate explicit user approval.
- Free public data has no institutional SLA. Missing, stale, or inconsistent data must stop trading rather than be guessed.

## Mature framework ownership

- RQAlpha is the target sole engine for virtual accounts, positions, orders, fills, transaction costs, A-share constraints, backtests, and simulated matching.
- Microsoft Qlib is the target framework for factors, model training, predictions, ranking, and experiment records.
- Models rank and score stocks. Rules and the RQAlpha engine decide whether and how a simulated trade can occur.
- Do not modify framework core code unless the user separately approves a documented exception. Prefer public APIs, configuration, adapters, and extension hooks.
- Supabase stores configuration, audit data, immutable results, and read models. It must not become a multi-call, non-atomic trading engine.
- The React frontend reads confirmed engine output. It must not independently calculate or write authoritative cash, holdings, fills, or realized PnL.

# Persistent Working Rule

When the user asks about this quant platform in a future session:

1. Read this file first.
2. Continue from the first incomplete roadmap item.
3. Before implementing that item, explain the exact intended file, workflow, schema, and data changes and wait for explicit confirmation.
4. Complete only the confirmed item.
5. Run acceptance checks proportional to its risk.
6. Update this file with status, date, changes, verification evidence, and remaining work.
7. Do not start the next roadmap item until the user confirms.

Status values:

- `Pending`
- `In Progress`
- `Completed`
- `Blocked`

If an item is partially complete, use `In Progress` and list the remaining work. Do not mark an item `Completed` based only on code existence, a successful build, or a dry run. Production-facing acceptance evidence is required.

# Product Definitions

- Strategy suggestion: a recommendation generated by rules or models. It is not an executed trade.
- Executed order: a virtual order confirmed by the authoritative simulation engine.
- Manual record: a user-recorded simulated operation. It is not automatically an engine fill.
- Today PnL: current open holdings' daily mark-to-market PnL.
- Floating PnL: open holdings' cumulative PnL versus cost.
- Realized PnL: PnL from closed or reduced simulated trades.
- Total PnL: floating PnL plus realized PnL.
- Total assets: initial capital plus total PnL.
- Authoritative ledger: the immutable, reconciled set of engine-generated orders, fills, cash changes, positions, and snapshots.
- Read model: a query-optimized Supabase representation derived from the authoritative ledger for the UI.
- Shadow simulation: a fully online virtual account that cannot affect any real asset and is not yet promoted as the main simulated strategy.

Today PnL formula:

```text
Today PnL = sum(shares * (current_price - previous_close)) for current open holdings
previous_close = current_price / (1 + change_rate / 100), if previous_close is unavailable
```

Missing market data must not be guessed. If a holding cannot be matched to current market data, count it as 0 for Today PnL, expose the missing-data issue, and block any decision that depends on the missing value.

# Target Cloud Architecture

```text
GitHub Actions / cloud runner
  -> online A-share data acquisition
  -> data freshness, coverage, and consistency gates
  -> Supabase Storage compressed point-in-time datasets
  -> Qlib factor/model ranking
  -> RQAlpha backtest and paper-simulation engine
  -> order/fill/cash/position/equity reconciliation
  -> atomic publication of an immutable run
  -> Supabase read models
  -> React online console

Supabase Cron / watchdog
  -> heartbeat check
  -> missed-run recovery trigger
  -> failure and quota alerts
```

GitHub Actions cache is a performance cache only and is never an authoritative data store.

# Historical Audit Baseline

The legacy platform is not trusted as the V2 accounting or strategy baseline. Preserve these audit facts until M0 is completed:

- Legacy `stock_trade_history` contained approximately CNY 38,014.93 of duplicated phantom losses caused by repeated failed close attempts.
- The final M0.2 frozen evidence contains one additional post-audit phantom loss of CNY 2,212.96, bringing the reproducible M0.3 polluted-record total to CNY 40,227.89 across 18 records. The earlier CNY 38,014.93 figure remains a dated audit observation rather than the final frozen total.
- Order-ledger-based realized loss was approximately CNY 66,554.39, with an additional open floating loss of approximately CNY 2,216.25 at the time of audit.
- Fifteen legitimate filled exits contained five wins and ten losses; approximate profit factor was 0.213.
- The three largest legitimate losses contributed approximately 89% of legitimate realized loss.
- The legacy strategy therefore remained economically unprofitable even after removing duplicated accounting records.
- Data, factor, model, and backtest pipelines had stale dates, incomplete coverage, zeroed factor fields, and non-professional metric or sensitivity implementations.

These figures are audit evidence, not permanent account truth. M0 must produce a dated, reproducible reconciliation report.

# V2 Master Roadmap

## M0: Freeze Legacy System And Preserve Evidence

Status: Completed

Completion note:
- 2026-07-15: Completed the full M0 legacy freeze, immutable evidence capture, deterministic pollution classification, and reproducible frozen account baseline without deleting or rewriting legacy evidence.
- Verification: M0.1 through M0.4 each passed their production-facing acceptance checks. The legacy source remains frozen at 350 records, and the next permitted roadmap item is M1 only after separate user confirmation.

Goal: Stop the legacy engine from creating additional unreliable state and establish a reproducible account baseline without deleting evidence.

### M0.1 Freeze legacy automatic execution

Status: Completed

Completion note:
- 2026-07-15: Implemented fail-closed guards in the legacy task runner, paper engine, model engine, React repository, and stock dashboard; removed the manual paper-trade workflow option; added a visible read-only warning; and applied online migration `20260715025450_freeze_legacy_stock_ledgers` with write-blocking triggers on eight legacy ledger tables.
- Verification: 55 related Python tests and 42 frontend tests passed, the production build and Python compilation passed, all eight online triggers were present, a service-role write probe was rejected without leaving a row, GitHub Pages deployment run `29385529464` succeeded, and the deployed asset exposed the legacy-account freeze banner.

Required work:

- Disable new automatic buy and sell execution from the legacy paper-trading workflow.
- Preserve read-only access to existing pages, tables, logs, and workflow history.
- Do not delete or rewrite historical rows.
- Display an explicit legacy-account trust warning where applicable.

Acceptance:

- A scheduled legacy run cannot create a new order, trade, position, or snapshot.
- Read-only history remains accessible.
- Manual verification and an automated fixture prove the freeze.

### M0.2 Capture immutable forensic backup

Status: Completed

Completion note:
- 2026-07-15: Added deterministic legacy-ledger export, per-table and per-file SHA-256 hashing, live schema capture, current account snapshots, recent GitHub Actions log capture, private content-addressed Supabase Storage upload, append-only online evidence manifests, and a controlled cloud capture workflow.
- Online evidence `legacy-20260715T031710984732Z-2ddcee6336bb` captured 350 rows across all eight frozen tables plus three workflow logs, live schema, migrations, workflow definitions, and current account snapshots. Private archive `sha256/2d/2ddcee6336bb189a24eda7522e49dd8924e56b8c4bf184a78f4636208044e715.zip` is 102,020 bytes and was independently re-downloaded with an identical SHA-256. Cloud run `29386154729` succeeded, its non-sensitive manifest artifact was present, and a service-role mutation probe was rejected by the append-only trigger without changing the record.
- Immutability boundary: Supabase Storage does not provide object lock or versioning. Tampering is made evident through content-addressed storage, an append-only database manifest, source-commit anchoring, and the separate GitHub Actions manifest artifact.

Required work:

- Export relevant legacy tables, schema definitions, workflow definitions, job logs, and current account snapshots.
- Record row counts, content hashes, export time, and source version.
- Store the backup in an online location that does not depend on the local computer.

Acceptance:

- Export counts reconcile to the source.
- Hashes are recorded and re-verifiable.
- No source data was changed during export.

### M0.3 Classify polluted records

Status: Completed

Completion note:
- 2026-07-15: Added deterministic classification from fixed M0.2 evidence, source-record and result hashes, reversible dispositions, append-only run and record tables, an atomic idempotent publication RPC, and a serialized cloud reconciliation workflow. Classified all 350 records as 32 authoritative candidates, 17 legitimate non-fill audit events, 15 legitimate derived trade projections, 18 polluted exclusions, and 268 reference-only read-model rows.
- Verification: Five deterministic fixtures and related M0 tests passed. Cloud runs `29400568287` and `29400634579` produced classification hash `37134ef5cb32717ec28ace054b9f156fa0c27f3c6c705cd7967b21d475c661ce`; the second run returned `idempotent_replay=true`, leaving one run and 350 mappings. All 18 excluded records have evidence-backed reasons and hashes, service-role mutation probes were rejected, review-required count is zero, and legitimate filled orders excluded is zero.
- Remaining limitations: M0.3 classifies records but does not yet rebuild cash, positions, or PnL. The reproducible final polluted PnL is CNY -40,227.89; it exceeds the earlier dated audit figure by the final CNY -2,212.96 phantom record written before M0.1 froze the legacy ledger.

Required work:

- Identify duplicated or partial records through deterministic rules.
- Mark them through a reversible audit classification or separate reconciliation mapping.
- Do not physically delete evidence.

Acceptance:

- Every excluded record has an evidence-backed reason.
- Re-running classification produces the same set.
- Legitimate orders are not excluded.

### M0.4 Rebuild legacy account baseline

Status: Completed

Completion note:
- 2026-07-15: Added deterministic account replay from the fixed M0.2 archive and M0.3 classification, original-versus-reconciled comparisons, four append-only service-only baseline tables, an atomic `SECURITY INVOKER` publication RPC, and a GitHub Actions cloud rebuild workflow. Acquisition fees are capitalized into book cost, sell fees reduce realized PnL, recorded slippage is disclosed but not deducted twice, and missing frozen valuation fails closed.
- Verification: Six M0.4 accounting and schema fixtures, five M0.3 fixtures, four evidence fixtures, four freeze fixtures, and Python compilation passed. Cloud runs `29403040639` and `29403295417` succeeded with baseline `legacy-baseline-legacy-account-baseline-v1-2adf9fceb123` and SHA-256 `2adf9fceb123ff4a38433c183add3718b9b56269a61fdf8b0daaa7e1c0a84d3a`; the second returned `idempotent_replay=true`, leaving one run, two accounts, 32 unique fill-surrogate entries, and six positions. All cash/equity, initial-capital/PnL, and final-position differences are CNY 0.00; four service-role mutation probes were rejected; and all 350 frozen source records remained unchanged.
- Reconciled result: the main account has cash CNY 913,162.06, market value CNY 18,063.00, realized PnL CNY -66,588.41, floating PnL CNY -2,186.53, total PnL CNY -68,774.94, and total assets CNY 931,225.06, compared with the polluted original total assets of CNY 891,037.47. The model account has total assets CNY 999,502.04. Original polluted metrics and the stale latest stored snapshots remain visible in the immutable report.
- Remaining limitations: the legacy system has filled-order evidence rather than a separate fill table, so the 32 replay events are explicitly labelled `filled_order_surrogate`. Valuation is a dated historical baseline from the frozen M0.2 position read model, not a current market valuation or evidence of strategy profitability. Post-migration advisor findings are limited to intentional RLS-with-no-public-policy notices, expected unused fresh indexes, and a low-volume unindexed classification foreign key; no new security warning was introduced.

Required work:

- Recalculate cash, positions, realized PnL, floating PnL, total PnL, and total assets from legitimate events.
- Produce original-versus-reconciled comparison.
- Freeze the reconciled legacy account as a historical reference account.

Acceptance:

- Orders, fills, cash, positions, and equity reconcile exactly.
- The report is reproducible from the forensic backup.
- Original polluted figures remain visible for audit purposes.

## M1: Cloud-Only Foundation And Cost Safety

Status: Completed

Completion note:
- 2026-07-16: Completed the M1 cloud-only runtime and cost-safety foundation. GitHub Actions now owns the scheduled foundation heartbeat and recovery workflow with pinned action commits, runtime versions, timeouts, retries, concurrency locking, and manual acceptance mode. Legacy stock schedules are manual diagnostics only. Development, shadow, and disabled main-simulation accounts are isolated and simulation-only; runtime tables and RPCs are service-role-only; and internal hard-budget quotas fail closed.
- Verification: Ten local runtime tests, four legacy-freeze tests, six legacy-baseline tests, 42 frontend tests, the production build, Python compilation, dependency audit with zero known npm vulnerabilities, SQL parsing, and migration checks passed. Cloud runs `29466102292` and `29466378290` both passed runtime and Python 3.11 framework-smoke jobs with RQAlpha 6.2.1 and Qlib 0.9.7. Each cloud run made ten duplicate claims but stored one business result; the repeated acceptance artifacts in run `29466378290` had identical SHA-256 `073dc8591024534d35bd542ced4bbe47176f0be246acb1058314750142d50adf`. Quota tests allowed 79%, blocked nonessential work at 80%, allowed only critical work at 90%, blocked all work at 100%, and reset the test override. An online stale-heartbeat probe created and completed recovery `648b172f-86d9-407b-8c7e-59d55aed3268`. Supabase Cron job `cloud-runtime-health-monitor` succeeded at 2026-07-16 02:20 UTC; all six runtime tables had RLS; anon/authenticated RPC execution was absent; the new mutable-search-path advisor warning was fixed; and an event-mutation probe was rejected by the append-only trigger. GitHub Pages run `29466378267` also succeeded.
- Remaining limitations: Quota telemetry is a conservative internal hard-budget counter, not a provider billing-dashboard API. It prevents configured work before the internal ceilings but cannot guarantee that a provider will never change its free-tier terms. Future daily market data, RQAlpha simulation, and Qlib research jobs remain disabled until their ordered M2-M6 milestones are accepted; the main simulation account remains disabled.

Goal: Run the complete production workflow online without relying on the user's computer or creating uncontrolled costs.

Required work:

- Define GitHub Actions cloud jobs, timeouts, concurrency locks, retries, and manual triggers.
- Add Supabase Cron heartbeat monitoring and missed-run recovery.
- Pin Python, Node, RQAlpha, Qlib, and critical data dependencies.
- Separate development, shadow, and main simulation accounts.
- Store secrets only in GitHub Secrets or Supabase Vault.
- Enforce RLS on exposed tables and prevent frontend service-role exposure.
- Add free-quota telemetry and hard stop rules.

Acceptance:

- A full cloud run succeeds while the local computer is offline.
- Duplicate cloud triggers do not create duplicate business results.
- A missing scheduled run is detected and recoverable.
- Quota thresholds stop jobs instead of creating charges.

## M2: Point-In-Time A-Share Data Foundation

Status: In Progress

Completion note:
- 2026-07-16: Completed and accepted the bounded M2.1 source-admission slice only: strict unadjusted daily-bar contracts, AKShare/Eastmoney primary and BaoStock secondary adapters, a fixed 20-symbol by 60-trading-day sample, fail-closed quality gates, deterministic non-authoritative manifests, and a manual-or-code-change cloud acceptance workflow with no Supabase or account credentials.
- Verification: Nine deterministic M2.1 tests and related M0/M1 regression checks passed. Two independent local live captures and GitHub-hosted runs `29468284614` and `29468422977` for business date 2026-07-15 each produced 1,200 rows per source, no source failures, 100% primary and cross-source pair coverage, zero duplicate or OHLC errors, and 100% cross-source close/volume-unit/amount consistency with pinned AKShare 1.18.64 and BaoStock 0.9.2. Both cloud artifacts had identical dataset SHA-256 `735a02ec7a4ef483f1d634d3c8796e2002e18b80f9fa648304ab4df0ba35308f` and quality SHA-256 `255833c13bb16f0accc7e915c7972caeaa95e02af04220efc8664ade965a0307`.
- Remaining limitations: M2.1 admits these two free sources only for the next bounded development step; it does not establish an institutional SLA or authorize production simulation data. The evidence is explicitly non-authoritative and cannot create simulated orders. Historical point-in-time universes, corporate actions, fundamentals, storage publication, daily scheduling, and Qlib/RQAlpha adapters remain pending M2 work. The next permitted item is M2.2 historical point-in-time universe and trading-calendar work only after separate user confirmation.
- 2026-07-16: Completed and accepted the bounded M2.2 historical point-in-time universe and trading-calendar slice. Added exact AKShare/Sina versus BaoStock calendar reconciliation, official CSI 300/500 current snapshots, official regular and temporary adjustment evidence, PDF/XLSX parsers, a separately evidenced `300114` to `302132` identifier-continuity event, reverse point-in-time reconstruction to 2018-01-01, pinned attachment hashes, fail-closed inventory and consistency gates, deterministic compressed evidence, and a cloud acceptance workflow. No database, account, strategy, order, or scheduled-production write was added.
- Verification: Four deterministic M2.2 tests, nine M2.1 regression tests, 29 M0/M1 regression tests, Python compilation, dependency resolution, and two independent local live rebuilds passed. The live calendar contained 2,069 exactly aligned sessions through 2026-07-15; 26 reviewed events produced 56 snapshots with exactly 300 CSI 300 and 500 CSI 500 members and zero overlap. Repeated local rebuilds had identical calendar SHA-256 `275d1eff877f87c66245445ccd8b2915df6cd5aa7b78525969cbf136be678b8b`, event SHA-256 `e496fcfe680f463e1b00fe18e747f36c8138bf48a1771414aee2981d5ef60832`, and snapshot SHA-256 `f9ced34ab8bd1e619792cdc7d5113c713fc2b7fdf573649f6240d733b56c6b84`. GitHub-hosted run `29473488555` succeeded on commit `6101d56`, produced one 22.7 KB evidence artifact with artifact SHA-256 `4135670fca87fb06f3e47cb639ff74eee0cd84417d92cd5ff9ccc56f174da8fc`, and required no local computer or secrets.
- Remaining limitations: M2.2 covers trading sessions and CSI 300/500 membership intervals only. It is non-authoritative, cannot create simulated orders, and does not yet provide full-universe daily bars, adjustment factors, corporate-action price continuity, ST/suspension/listing-age/limit-price facts, industry, fundamentals, flow data, cloud storage publication, or scheduled production ingestion. Free public sources still have no institutional SLA. At the time of M2.2 acceptance, M2.3 had not started.
- 2026-07-16: M2.3 remains `In Progress`. Implemented non-authoritative historical raw/QFQ/HFQ bars, adjustment factors, security references, suspension/ST/listing-age/price-limit/one-price tradeability facts, dual-source verification, deterministic compressed evidence, fail-closed quality gates, observable cloud shards, per-request and per-symbol time limits, bounded retries, shard checkpoints, and deterministic shard merging. Relevant commits are `f06655e`, `a997fd6`, and `359866c`; no database, account, strategy, order, or simulated fill write was added.
- M2.3 verification to date: 29 local deterministic and regression tests passed; the repaired cloud 20-symbol sample job in run `29488301120` passed. Earlier 100-symbol preflight run `29479669942` correctly failed closed and exposed legal suspended rows with blank volume, unchanged adjustment-factor metadata dates, transient Sina verification failure, and an oversized shard timeout. Those causes were addressed by excluding explicit suspended status rows from trading bars, separating adjusted OHLC from volume, checking only actual factor changes, increasing staggered verification retries, reducing shards to 10 symbols, enforcing a 90-second cloud symbol deadline, using at most four concurrent shards, and applying a 10-minute shard limit.
- Shutdown checkpoint: cloud 100-symbol preflight run `29488580735` is the current acceptance run. At the final pre-shutdown check, deterministic tests and planning had passed; shard `1` had succeeded, shard `2` had failed, shards `0`, `3`, `4`, and `5` were running, and shards `6` through `9` were queued. The next session must inspect the completed run and shard `2` logs/artifact first, diagnose the exact failed gate, and wait for confirmation before editing. If a later rerun passes, verify all 10 shard manifests and the merged hashes, then prepare the separate full 1,403-symbol M2.3 acceptance; do not mark M2.3 complete based on the 100-symbol preflight alone.
- 2026-07-23: Added TiDB Cloud Starter as a candidate zero-cost-first historical-market warehouse for M2.3 stabilization. The initial reachable instance is in Tokyo / `ap-northeast-1` and was verified with a read-only `SELECT VERSION(), CURRENT_USER(), DATABASE()` probe against TiDB `8.0.11-TiDB-v8.5.3-serverless`. TiDB is intended only as a candidate storage layer for large historical market-data checkpoints; Supabase remains the control-plane/audit/read-model system unless separately approved.
- TiDB credential rule: never commit TiDB passwords, root credentials, DSNs, or secrets into tracked files or documentation. Local credentials may exist only in ignored `.env.local`; cloud credentials must be stored only in GitHub Secrets or another approved secret manager. The currently tested TiDB root password was pasted in chat during setup, so it should be rotated before any production-like or long-lived use.
- 2026-07-23 shutdown checkpoint: User paused work for the day while the TiDB M2.3 storage slice was in progress. Uncommitted local changes added `scripts/market_data/tidb_checkpoint_store.py`, `scripts/market_data/publish_historical_to_tidb.py`, `scripts/test_tidb_checkpoint_store.py`, optional `publish_tidb` wiring in `.github/workflows/market-data-history-acceptance.yml`, `PyMySQL==1.1.1` in `scripts/market_data/requirements.lock.txt`, and reduced nested AKShare retry pressure in `scripts/market_data/historical_bars.py`. Verification completed before pause: Codex bundled Python successfully ran `python -m scripts.test_tidb_checkpoint_store` with six passing tests and `py_compile` for the new/modified Python files. Work paused before installing/verifying the full lock file, before creating the TiDB `chef_menu_market` schema, before local sample insertion, before full M2.3 regression tests, before GitHub Secrets configuration, and before any commit or push. Next session should resume by installing `scripts/market_data/requirements.lock.txt` into the active Python environment, running M2.3 regression tests, creating/verifying the TiDB schema locally, inserting one existing `.codex_tmp/m2-history-sample-*` artifact, checking repeat insertion idempotency, and only then asking for confirmation before enabling an online GitHub run with `publish_tidb=true`. Do not mark M2.3 complete from this storage slice.
- 2026-07-24: Completed the local TiDB M2.3 storage-checkpoint validation slice. Implemented forced-TLS TiDB connection, idempotent schema creation, run/bars/tradeability/adjustment/reference/symbol-checkpoint upserts, old-artifact compatibility for missing `verification-checks.json.gz`, default three-attempt publish retries with rollback, and optional GitHub Actions `publish_tidb` steps guarded by repository secrets. Local database `chef_menu_market` was created on TiDB Cloud Starter and `.env.local` now points to it while remaining git-ignored. Verification: installed `scripts/market_data/requirements.lock.txt` into the active Codex Python environment; `scripts.test_tidb_checkpoint_store` passed eight tests; `py_compile` passed for new/modified Python files; M2.3 regression tests passed for market-data contracts, quality, calendar, point-in-time universe, historical market data, historical quality gates, shard merge, adjustment engine, tradeability, BaoStock calendar, and BaoStock history. Real TiDB sample publication used dataset `local-20260724-m2-history-sample-v4` from `.codex_tmp/m2-history-sample-v4`; initial publish, repeat publish, and final database probe confirmed one run, 20 successful symbol checkpoints, zero failed checkpoints, 1,940 historical bars, 1,940 tradeability facts, 429 adjustment events, 20 security references, `accepted=1`, and `simulation_orders_allowed=0`. Remaining before online acceptance: commit/push the storage slice, configure GitHub Secrets, run a workflow_dispatch sample or preflight with `publish_tidb=true`, inspect TiDB checkpoint counts, then decide whether to proceed to larger full-universe M2.3 acceptance. Do not mark M2.3 complete from the local TiDB storage slice alone.
- 2026-07-27: Implemented and locally verified the M2.3 resumable cloud-acquisition repair after workflow run `30061729403` exposed three failed preflight shards and skipped TiDB publication. Historical acquisition now writes an atomic TiDB checkpoint after every attempted symbol, stores cross-source verification rows, reconstructs successful symbol evidence from normalized TiDB tables, validates resumed evidence against the frozen expected-key scope, skips already successful primary and verification requests, retries only failed or missing symbols once inside the same GitHub job, and reuses a stable dataset id derived from business date, deterministic plan hash, and shard coordinates across later workflow reruns. Verification calls and primary bundle calls are each bounded by the 60-second Linux symbol deadline. Workflow defaults now select the bounded `sample` mode and `publish_tidb=true` for manual dispatch, while push-triggered samples remain non-publishing; unaccepted partial and merged evidence remains explicitly non-authoritative and `simulation_orders_allowed=false`.
- 2026-07-27 verification: 57 deterministic M2.3 and prerequisite tests passed, including new checks that a symbol checkpoint cannot create an incomplete aggregate run, duplicate checkpoint writes remain idempotent, successfully resumed symbols do not call either market-data endpoint again, and incomplete verification checkpoints are refreshed. Python compilation, YAML parsing, and `git diff --check` passed. Real TiDB dataset `local-20260727-resume-checkpoint-v2` passed first write, repeat write, and read-back reconstruction with one final symbol checkpoint, one verification row, one resumed symbol, and no simulated-order permission. The repair was committed and pushed as `8ffa5be`; local verification alone does not complete M2.3.
- 2026-07-27 online sample acceptance: GitHub Actions run `30233548539` passed on commit `8ffa5be` for business date `2026-07-24` with `publish_tidb=true`. All 20 sample symbols and all 20 cross-source verification requests succeeded; the accepted evidence contained 2,080 historical bars, 2,080 tradeability facts, 2,080 verification checks, 439 adjustment events, 20 security references, and 20 successful TiDB symbol checkpoints. TiDB dataset `gh-30233548539-sample-2026-07-24` reported one accepted aggregate run and `simulation_orders_allowed=false`. Artifact `8640909256` was uploaded with SHA-256 `018830e739901a4cf965e28456ba8fb786ba2bfda11f7688b8cf68e70dc37431`. Remaining before larger acceptance: run a bounded 100-symbol `preflight` with TiDB checkpoints, inspect any failed quality gate without discarding successful symbols, then repeat the same deterministic scope to verify checkpoint reuse and final reconciliation. M2.3 remains `In Progress`; this sample is not full-universe acceptance or production simulation authorization.
- 2026-07-27 online 100-symbol preflight acceptance: Fixed the quality-gate scope defect exposed by run `30233997312`, where arbitrary shard 6 had 97.99% local coverage even though the complete 100-symbol scope exceeded the preregistered 98% requirement. Commit `50e20a5` now records shard-local aggregate coverage and corporate-action checks as non-critical diagnostics, recomputes both as critical gates after deterministic merge, and still treats unknown status as active/missing unless suspension is explicitly confirmed. It does not lower the 98% threshold, infer suspension, fabricate bars, or permit trading on missing data. Eleven focused tests and 63 M2.3/prerequisite regression tests passed locally, including global-pass/shard-fail concentration, global coverage rejection, exact 98% boundary, confirmed-suspension versus unknown-status handling, incomplete cross-source verification, and preservation of unrelated shard failures.
- 2026-07-27 cloud verification: GitHub Actions run `30239636586` passed on commit `50e20a5` for business date `2026-07-24`, mode `preflight`, and `publish_tidb=true`. Deterministic tests, the plan, all ten 10-symbol shards, and the final merge succeeded. Shard 6 resumed all ten symbols and seven verification symbol bundles from TiDB with no market-data request, retained its 97.99% local coverage result as a visible non-critical diagnostic, and published an accepted shard with 9,626 bars, 9,823 tradeability facts, 5,295 verification checks, and 165 adjustment events. The merged manifest `m2-historical-market-merged-manifest-v2` was accepted with complete and consistent shard inventory, zero cross-shard duplicates, reconciled counts, accepted cross-source gates, and accepted global aggregate gates. Its global coverage was 127,464 / 128,142 (99.47%), and corporate-action adjustment alignment was 1,525 / 1,531 (99.60%). The merged bar SHA-256 `ddc15b01db146f892aaf74d0a0e458ab32e381ab67b40a55e35bea5e80ecdf0a` and tradeability SHA-256 `9b1f226a13cbd64630b8604ab8af6b4a0d2a74847ceb2119ea5e57d08d8088ea` exactly matched failed run `30233997312`, proving the repair changed quality-gate scope without changing the captured market evidence. TiDB dataset `m2-preflight-2026-07-24-3443e8f3b2ed61c6a4741fa5d9491a90f6414424eda1b8ef64d54b574043c3ac-merged` independently read back as one accepted non-authoritative run, 100 successful symbol checkpoints, 127,464 bars, 128,142 tradeability facts, 2,771 adjustment events, 100 references, and 49,202 verification checks; all 677 unknown-status rows remained blocked, with zero missing-data rows marked buyable or sellable and `simulation_orders_allowed=0`. Merged artifact `8642906191` has SHA-256 `f86650fb287071a0846e53c8a7cc1d1eabc17140569e406ab9056b51f2f2cb0a`. M2.3 remains `In Progress`; this bounded preflight is not full-universe acceptance or production simulation authorization.
- 2026-07-27 full-run storage checkpoint: Commit `b4552b4` upgraded TiDB checkpoint schema to `m2-tidb-market-checkpoint-v3`, added an idempotent merged-run-to-physical-shard mapping, and changed accepted merge publication to store logical counts, global hashes, and shard pointers without duplicating bars, tradeability facts, adjustment events, references, verification rows, or symbol checkpoints. Manifest-only publication now fails closed unless every critical merge check passes, every shard file count and SHA-256 reconciles, and every referenced TiDB physical shard is already committed, accepted, simulation-only, non-authoritative, and exactly matches its embedded manifest. GitHub uploads only the final merged manifest while retaining physical shard artifacts, avoiding a second approximately full-size artifact copy. All 71 M2.3 and prerequisite regression tests passed locally; workflow YAML parsing and Python compilation passed. A real TiDB sample for business date `2026-07-24` stored 20 physical symbols, 2,080 bars, 2,080 tradeability facts, 439 adjustment events, 20 references, and 2,080 verification checks under `local-20260727-m2-manifest-only-v3-shard-0-of-1`. Logical dataset `local-20260727-m2-manifest-only-v3-merged` was published twice idempotently with one shard mapping and independently queried as having zero physical rows in all six market/checkpoint tables, while retaining the correct logical counts and `simulation_orders_allowed=0`. Full 1,403-symbol GitHub Actions run `30243090884` was launched on commit `b4552b4` for business date `2026-07-24`, mode `full`, with 141 shards, `max-parallel=2`, and TiDB publication enabled. At this checkpoint deterministic tests and planning had passed, shards 0 and 1 had completed successfully, TiDB held 20 successful and zero failed symbol checkpoints for the full-run scope, and the next two shards were running. The next session must inspect run `30243090884` first; if it is incomplete, allow its online checkpoints to continue without starting a competing full run; if it failed, diagnose the exact failed shard or merge gate and reuse the same stable TiDB checkpoint scope; if it passed, verify all 141 physical shard runs, the manifest-only mapping count, logical-versus-physical reconciliation, hashes, missing-data trade blocks, artifact inventory, and current free-quota usage before recording full-universe acceptance.
- 2026-07-28 local full-run recovery repair: Diagnosed GitHub Actions run `30243090884` as 139 completed shards plus two job-level timeouts, not a GitHub outage or free-quota rejection. TiDB read-only reconciliation under frozen scope `993df9aab3cbd021a495535c9326eaa79f26f4bbfbe74b28215256e778e517f7` found exactly 1,326 structurally complete resumable symbols, 64 failed symbols, and 13 never-attempted symbols across 52 affected shards. Implemented a frozen-plan-derived repair matrix, a non-swallowable Linux symbol deadline, separate `capture`/`repair`/`finalize` policies, fail-closed verification-checkpoint refresh, nonpositive adjusted-row isolation without fabricated values, and checkpoint-manifest-only physical-run registration that avoids rewriting millions of existing rows. The `resume` workflow is designed to repair only the 77 missing or failed symbols at `max-parallel=2`, then rebuild all 141 final shard artifacts from TiDB with public market-data acquisition disabled before the existing deterministic merge.
- 2026-07-28 verification: All 75 deterministic M2.3/prerequisite tests passed, along with Python compilation, workflow YAML parsing, and `git diff --check`. Real read-only TiDB planning reproduced exactly 1,326 resumable symbols, 77 repair symbols, and 52 repair shards while retaining the original checkpoint scope hash. Real shard 0 finalization recovered 10/10 symbols, 18,075 tradeability rows, and 18,053 bars with `acquired_symbol_count=0` and an accepted manifest; its checkpoint-manifest-only registration preconditions independently passed against the stored TiDB inventory without writing a run row. Intentionally incomplete shard 27 failed before writing a manifest with the public-market-data prohibition message. No GitHub workflow was triggered and no M2.3 completion is claimed from local evidence.
- 2026-07-28 quality-aware recovery hardening: GitHub resume run `30323905791` showed that immediate retries alone could not recover archived identifiers, nonpositive additive QFQ rows, or missing independent verification. Added Tencent archive raw-history fallback with exact volume/amount units, separately attributed positive multiplicative Sina factor timelines, per-row raw/factor/verification provenance, strict primary-versus-verification source independence, quality-aware TiDB repair planning, atomic successful-symbol replacement that cannot leave stale adjustment or verification rows, and one aggregate post-repair workflow diagnosis instead of treating every unresolved matrix child as a separate root cause. Failed checkpoint attempts preserve prior physical rows and all outputs remain non-authoritative with `simulation_orders_allowed=false`.
- 2026-07-28 quality-aware verification: All 78 M2.3/prerequisite regression tests, Python compilation, workflow YAML parsing, and `git diff --check` passed. Live local source probes recovered all 23 primary-acquisition or targeted-verification symbols currently exposed by the frozen full-run inventory; `300114` used Tencent archive history, the other currently reachable symbols used Eastmoney or Sina, and every generated QFQ/HFQ price was positive. `600240` obtained an independent Tencent verification series when Eastmoney was excluded. TiDB schema `m2-tidb-market-checkpoint-v4` was applied idempotently, and repeated planning from both the original and an already enriched frozen plan retained scope hash `993df9aab3cbd021a495535c9326eaa79f26f4bbfbe74b28215256e778e517f7` and evidence hash `afe07dc1d2c54e537ab279a2faf248e493108bed83878825435bb31f720216f8` while identifying 1,341 resumable symbols and exactly 62 repair symbols across 55 shards: 13 prior primary failures, nine bar/factor inventory mismatches, 38 verification rows missing source attribution, and two incomplete verification inventories. No online workflow was triggered from this local acceptance and M2.3 remains `In Progress`.
- 2026-07-28 bounded archive and status-only recovery acceptance: GitHub resume run `30335454897` recovered 59 of the 62 planned symbols and left exactly `000939`, `002005`, and `600485` as primary failures, so the aggregate remained fail-closed at 1,400 / 1,403 resumable symbols. Frozen-plan diagnosis showed that each identifier was an index member only for the 105 sessions from 2018-01-02 through 2018-06-08 and BaoStock explicitly recorded `tradestatus=0` for every scoped session; their available price histories begin only after that membership interval. Implemented a bounded three-symbol archive with independently attributed Tencent prices, BaoStock prices/status/reference data, and Sina multiplicative factor evidence, dataset SHA-256 `8da70ee84d9646653ee54ed5622af768bdf081bedca9802b5c8482d4aed34771`, strict row/hash/source validation, and live-first fallback only inside the frozen 2018-01-01 through 2026-07-24 boundary. Zero bars are now resumable only when every expected fact is explicitly secondary-confirmed suspended, buy and sell are both blocked, the reference and source provenance are present, the bar hash is absent, and the tradeability inventory/hash exactly reconcile; missing or unknown status still fails closed. All 84 M2.3/prerequisite regression tests, Python compilation, workflow YAML parsing, `git diff --check`, and a credential-leak scan passed. A read-only integration against the real TiDB checkpoints and original 1,403-symbol frozen plan forced all live providers to fail and accepted shards 23, 64, and 129 as 9 resumed plus one archived symbol each, with zero fabricated bars, exactly 105 blocked tradeability facts, one reference, the frozen archive hash, and `simulation_orders_allowed=false`. This local acceptance made no TiDB write and does not complete M2.3; the next required evidence is an online resume and accepted final merge under the unchanged frozen scope.
- Remaining limitations: M2.3 has not passed full-universe production-facing acceptance. The new recovery workflow still requires one online `resume` run using frozen-plan source run `30243090884`; only its accepted 141-shard merge, TiDB logical/physical reconciliation, global gates, hashes, artifacts, and quota review can complete the full-universe acceptance. Daily incremental scheduling, industry, fundamentals, verified flow data, and Qlib/RQAlpha adapters remain pending. All M2.3 outputs remain non-authoritative and `simulation_orders_allowed=false`; any critical gate failure must continue to produce zero simulated orders.

Goal: Provide synchronized, auditable, point-in-time data suitable for A-share research and simulation.

Initial universe:

- CSI 300 plus CSI 500 constituents.
- Expand beyond the CSI 800 only after storage, data quality, and cost review.

Required work:

- Trading calendar, raw and adjusted OHLCV, amount, turnover, suspension, ST/delisting risk, listing age, limit prices, index data, industry mapping, corporate actions, fundamentals, and verified flow data.
- Store source, business date, fetch time, schema version, and content hash.
- Use compressed point-in-time datasets in cloud storage; keep query metadata and derived results in Postgres.
- Cross-check critical prices against a second source.
- Add freshness, coverage, duplicate, OHLC, price-limit, adjustment, and date-alignment gates.

Acceptance:

- Eligible-universe coverage is at least 98%.
- Critical business dates are 100% aligned.
- Deterministic data hashes and adjustment spot checks pass.
- Any failed critical gate produces zero new simulated orders.

## M3: RQAlpha Authoritative Simulation Engine

Status: Pending

Goal: Replace the legacy custom account and matching core with one mature authoritative engine.

Required work:

- Use RQAlpha for cash, positions, available shares, orders, fills, transaction costs, slippage, T+1, board lots, suspension, price limits, backtests, and simulated matching.
- Use adapters and extension hooks rather than editing RQAlpha core.
- Give every run, decision, order, and fill a stable unique idempotency key.
- Atomically publish a complete reconciled run; never publish a partial account.
- Support deterministic replay using immutable inputs, strategy version, and engine version.
- Retire legacy `paper_trade_engine.py` from authoritative account writes.
- Build daily position coverage from the authoritative set of all open RQAlpha positions, independently of candidate-list or model-ranking limits. Every open position must receive an explicit valuation, market-data state, and risk-evaluation state even when no current prediction exists.
- Accept simulated order instructions only through validated structured fields. Human-readable recommendation text must never be parsed as an authoritative order price, stop, target, quantity, or side.

Acceptance:

- Ten retries of the same business date create one business result.
- Order, fill, cash, position, fee, and equity reconciliation differences are zero.
- Deterministic A-share fixtures cover T+1, suspension, board lots, limit-up, limit-down, costs, slippage, rejection, and partial failure.
- Backtest and simulation use the same execution rules.
- A held stock outside the candidate list or persisted prediction Top-N still receives exactly one daily position evaluation. Missing market data is exposed and fails closed rather than silently omitting the position or guessing a value.
- An order request with missing or invalid structured price or quantity fields is rejected without changing cash, positions, orders, or fills.

## M4: Explainable A-Share Adaptive Baseline Strategy

Status: Pending

Goal: Establish a simple, transparent, risk-controlled baseline before complex machine learning.

### Universe gates

- Exclude ST, `*ST`, delisting risk, suspensions, insufficient liquidity, listing age below 120 trading days, untradeable one-price boards, recent consecutive limit-down risk, and the highest-risk volatility tail.
- Initial 20-day average amount threshold: CNY 100 million, subject to preregistered sensitivity testing.

### Market regimes and gross exposure caps

- Strong bull: 80%.
- Weak bull: 60%.
- Range: 40%.
- Bear: 20%.
- Critical risk: 0%.

### Baseline cross-sectional factor weights

- Residual momentum: 25%.
- Trend quality: 15%.
- Volume-price and liquidity structure: 20%.
- Verified capital flow: 15%.
- Quality and risk: 25%.

Factor exposures must be industry- and size-aware. Missing unverified flow data receives a neutral value, never a fabricated positive value.

### Portfolio rules

- Hold 8 to 12 stocks when enough valid candidates exist.
- Initial single-stock weight no greater than 8%.
- Single-industry weight no greater than 20%.
- Planned risk per stock no greater than 0.5% of account equity.
- Total planned open risk no greater than 3%.
- Keep cash when valid candidates are insufficient.
- Normal rebalance weekly; risk exits evaluated daily.
- No same-day re-entry after an exit.
- Ten-trading-day cooldown after a stop loss.

### Core exits

- Original risk stop.
- Cross-sectional rank deterioration.
- Time stop without excess return.
- Portfolio de-risking after market-regime deterioration.

### Structured recommendation contract

- Store entry-price lower and upper bounds, stop price, target prices, defensive price, business date, validity window, data version, strategy version, actionability state, and blocked reason as typed structured fields where applicable.
- Generate human-readable recommendation narratives from validated structured fields. Never reconstruct authoritative numeric fields by parsing narrative text.
- A recommendation that requires a price but lacks a valid structured price range, stop, or other required field is non-actionable and must expose the exact validation failure.
- Daily risk evaluation covers the union of all authoritative open positions and valid new-position candidates. Candidate ranking may limit new-buy consideration but must never remove an existing position from monitoring or exit evaluation.

Acceptance:

- Every recommendation records data version, regime, factors, contributions, risk budget, and reason.
- Candidate ordering is explicitly score-based and deterministic.
- The strategy can hold 100% cash.
- No single stock or small cluster can dominate planned account loss.
- Every actionable recommendation passes deterministic structured-field and price-relationship validation; narrative text and structured values cannot contradict each other.
- The number of authoritative open positions equals the number receiving a daily risk-evaluation result, including positions with no current rank or prediction.

## M5: Professional Backtest And Falsification

Status: Pending

Goal: Attempt to disprove the strategy under realistic A-share constraints before promotion.

Required work:

- Generate signals with information available at T and execute no earlier than T+1.
- Use point-in-time universes and corporate-action-safe data.
- Include fees, slippage, T+1, suspensions, price limits, board lots, liquidity, and failed fills.
- Use rolling train, validation, test, and untouched out-of-sample windows.
- Compare CSI 300, CSI 500, and eligible-universe equal weight.
- Report daily portfolio returns, annual return, excess return, max drawdown, Sharpe, Calmar, profit factor, turnover, holding period, consecutive losses, and tail concentration.
- Run parameter perturbation, doubled-cost, delayed-fill, missing-data, and regime stress tests.
- Reconcile every equity-curve change to cash flows and fills.
- Include regression scenarios where an existing holding falls outside the candidate universe or persisted prediction Top-N, or has a missing or stale prediction. The holding must remain valued and risk-evaluated, and missing required market data must fail closed.

Preregistered promotion gates:

- Positive after-cost out-of-sample excess return.
- Positive excess return in at least 70% of rolling out-of-sample windows.
- Out-of-sample profit factor at least 1.2.
- Out-of-sample Sharpe at least 0.8.
- Calmar at least 0.5.
- Maximum drawdown no greater than 15%.
- Doubled costs do not turn the strategy into a materially loss-making strategy.
- The three largest losses contribute less than 40% of total losses.
- Order/fill/equity reconciliation difference is zero.

Failure of a critical promotion gate prevents advancement. Do not tune thresholds after observing the result without creating a new experiment version.

## M6: Qlib Model Enhancement

Status: Pending

Goal: Add machine learning only when it improves the validated baseline out of sample.

Required work:

- Establish Alpha158 plus LightGBM as the first Qlib benchmark.
- Predict five-to-ten-day excess return relative to industry or CSI 500, not unadjusted next-day direction.
- Compare baseline factors, model-only ranking, and a preregistered factor/model blend.
- Train weekly and generate incremental daily predictions where appropriate.
- Store model name, version, training cutoff, feature version, dataset hash, score, rank, predicted excess return, and confidence.
- Fall back to the validated factor baseline when predictions are missing or stale.
- Do not permit model output to directly create a simulated order.
- Treat prediction Top-N as a candidate-ranking or presentation limit only. It must not limit daily valuation, baseline risk evaluation, or exit handling for authoritative open positions.

Acceptance:

- No future leakage.
- Predictions are reproducible by version and data hash.
- The blended strategy passes M5 gates and materially improves the baseline on untouched data.
- A model failure cannot corrupt or block the authoritative account.
- A held stock outside the model Top-N, or without a current prediction, still receives exactly one versioned baseline risk result and cannot become an unmonitored position.

## M7: Online Trust And Research Console

Status: Pending

Goal: Preserve the React UI while making it a read-only, explainable, cloud-accessible control and research console.

Required views:

- Account overview.
- Data health and coverage.
- Strategy and model versions.
- Candidate ranking and factor explanations.
- Suggestions versus orders versus fills.
- Cash, position, order, fill, and equity reconciliation.
- Performance and risk attribution.
- Backtest and benchmark comparison.
- Shadow-account comparison.
- Cloud job history, failures, heartbeat, and quota usage.
- Circuit-breaker status.
- Truthful cloud-job lifecycle showing the actual GitHub run id and link, trigger type, business date, started and heartbeat times, validation state, completion state, output run or dataset id, and failure or blocked reason.
- Retire the legacy page behavior that writes `stock_job_requests` and presents a saved database request as evidence that GitHub Actions has started. The V2 console remains read-only; scheduled production work and administrator manual dispatches originate from the approved cloud workflow.
- Distinguish scheduled, started, running, validating, succeeded, failed, data-incomplete or blocked, stale or timed-out, and canceled states without collapsing them into a generic healthy state.

Acceptance:

- Every displayed business number traces to run id, business date, engine version, strategy version, and data version.
- The frontend cannot authoritatively mutate cash, fills, or positions.
- Failed or stale states are prominent and cannot appear healthy.
- Simulation-only and non-investment-advice labels remain visible.
- A displayed task can be traced to a real cloud run and its business output. Saving a request record alone can never be displayed as started, running, or successful.
- Data-incomplete, blocked, timed-out, and canceled jobs remain visibly distinct and cannot publish or imply a successful business result.

## M8: Sixty-Trading-Day Cloud Shadow Simulation

Status: Pending

Goal: Validate online behavior and operational reliability without any real-money exposure.

Required work:

- Run a separate V2 shadow account fully online for at least 60 trading days.
- Keep the legacy account frozen and read-only.
- Reconcile daily and compare simulation behavior with deterministic replay.
- Review weekly differences between expected backtest behavior and observed simulation behavior.
- Spot-check representative A-share constraint scenarios against a mature hosted platform where feasible.

Circuit breakers:

- Daily portfolio loss reaches 1%: stop new positions for that day.
- Drawdown reaches 5%: halve new risk allocation.
- Drawdown reaches 8%: stop new buys.
- Drawdown reaches 10%: freeze the strategy for review.
- Five consecutive losses: pause and diagnose.
- Any critical data, reconciliation, or cloud-job failure: create zero new orders.

Acceptance:

- No duplicate accounting for 60 trading days.
- No stale-data trade.
- No unexplained cash or position change.
- Replay and shadow-account business results reconcile.
- Risk exposure follows the preregistered strategy.
- Positive short-period returns alone cannot override operational or risk failures.

## M9: Main Simulation And Continuous Research

Status: Pending

Goal: Promote only a fully accepted strategy to the main virtual account and operate a disciplined research lifecycle.

Required work:

- Champion strategy runs the main simulation account.
- Challenger strategies run isolated shadow accounts.
- Promotion requires a new M5 validation package and M8 shadow acceptance.
- Review monthly attribution, turnover, execution cost, sector exposure, factor decay, and drawdown.
- Review quarterly whether the strategy remains valid.
- Retain immutable strategy versions and retirement reasons.

Acceptance:

- Promotion and retirement are versioned, auditable, and reversible.
- Challenger results cannot contaminate the champion account.
- The platform remains simulation-only.

# Roadmap Execution Order

Execute strictly in this order unless the user explicitly approves a change:

```text
M0.1 -> M0.2 -> M0.3 -> M0.4
-> M1 -> M2 -> M3 -> M4 -> M5 -> M6 -> M7 -> M8 -> M9
```

The first incomplete item is `M2 Point-In-Time A-Share Data Foundation`. M2.1 and M2.2 are completed; the next bounded M2 slice requires separate planning and user confirmation.

# Completion Record Format

After completing an item, add a note directly beneath its status:

```text
Completion note:
- YYYY-MM-DD: What changed.
- Verification: Tests, online checks, reconciliation evidence, and relevant run ids.
- Remaining limitations: Any non-blocking caveats.
```

Do not start the next item until the user confirms.
